language: python
python:
  - "3.8"

# Install dependencies
install:
  - pip install black

jobs:
 include:
  - stage: formatter
    script: black --check example.py

# Run the script
  - stage: display
  - script:
    - echo 'DONE'
# if: CHK==1 than python example.py
  
