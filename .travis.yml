language: python
python:
  - "3.8"

# Install dependencies
install:
  - pip install black

# Run the script
script:
- black --check example.py
- CHK=$(echo $?)
- echo $CHK
if: $CHK==1 than python example.py
  
