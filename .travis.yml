language: python
python:
  - "3.8"

# Install dependencies
install:
  - pip install black

# Run the script
script:
- black --check example.py
- CHK=$(echo $?)
- echo $CHK
# if: $check = 0 than python example.py
  
